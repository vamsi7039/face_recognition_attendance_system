Face Recognition Attendance System
This project implements a face recognition attendance system using CNN and transfer learning with TensorFlow/Keras.

Setup
Create a Python 3.13.9 environment.
Install required packages: tensorflow, opencv-python, numpy, matplotlib.
Run train.py to train or fine-tune the model.
Run attendance.py to detect faces and mark attendance.
